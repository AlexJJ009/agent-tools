# Win11 proxy relay

The architecture, migration guide, installer, runtime, dashboard, and tests have
moved to the canonical private repository:

<https://github.com/AlexJJ009/win11-proxy-relay>

This repository keeps only this pointer so the implementation cannot silently
diverge between `agent-tools` and the deployed Win11 machines.

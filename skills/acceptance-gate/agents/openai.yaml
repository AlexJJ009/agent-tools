interface:
  display_name: "Acceptance Gate"
  short_description: "Check scoped readiness, current evidence and completion"
  default_prompt: "Use $acceptance-gate to check action conditions and phase completion without conflating decisions, authority and user acceptance."
policy:
  allow_implicit_invocation: true

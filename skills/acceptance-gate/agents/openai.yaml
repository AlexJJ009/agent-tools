interface:
  display_name: "验收与人工复核"
  short_description: "检查证据、状态、人审和正式运行门禁"
  default_prompt: "Use $acceptance-gate to verify checklist evidence, review status, candidate SHA, and formal-run authorization before a protected action."
policy:
  allow_implicit_invocation: true

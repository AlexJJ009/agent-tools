interface:
  display_name: "Behavior-Preserving Cleanup"
  short_description: "Improve the current diff and preserve its agreed behavior"
  default_prompt: "Use $cleaner to clean the current diff, retain applicable decisions, and recheck affected evidence."
policy:
  allow_implicit_invocation: true

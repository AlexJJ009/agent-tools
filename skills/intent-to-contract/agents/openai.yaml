interface:
  display_name: "需求与协议入口"
  short_description: "读回目标、关键参数、协议和验收清单"
  default_prompt: "Use $intent-to-contract to turn this request into a grounded task agreement with protocol bindings and acceptance criteria."
policy:
  allow_implicit_invocation: true

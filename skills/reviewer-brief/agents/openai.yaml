interface:
  display_name: "Reviewer Brief"
  short_description: "Explain a bounded choice or independent review target"
  default_prompt: "Use $reviewer-brief to prepare a focused decision brief from the current workflow state and concrete observations."
policy:
  allow_implicit_invocation: true

# Fixture Source Notes

These fixtures model local work-report outputs for a skill implementation task.

The original request asked for a local Markdown reporting skill with deterministic local checks, a separate Judge review JSON, frozen request and state inputs, Git output exclusion safety, and stale-artifact finalization protection. It explicitly excluded databases, publishing, and schedulers.

The report bodies intentionally omit test answer labels so a reviewer can assess the artifact from the report content and supplied source context.

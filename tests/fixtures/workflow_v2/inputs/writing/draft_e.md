# Refresh decision

The provenance-aware readback boundary provides a warrant for the acceptance
contract through an invalidation-sensitive observation layer. The cache
mechanism preserves semantic alignment between runtime and operational truth.
This supports the selected option and the next governance checkpoint.
